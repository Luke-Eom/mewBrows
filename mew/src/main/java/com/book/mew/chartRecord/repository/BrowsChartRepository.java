package com.book.mew.chartRecord.repository;

import com.book.mew.chartRecord.entity.BrowsChart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrowsChartRepository extends JpaRepository<BrowsChart, Long> {
}
