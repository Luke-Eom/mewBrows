package com.book.mew.chartRecord.repository;

import com.book.mew.chartRecord.entity.BrowsRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrowsRecordRepository extends JpaRepository<BrowsRecord, Long> {
}
